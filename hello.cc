#include <iostream>
#include <string>

int sum(int a, int b) {
    return a + b;
}

int sum(int a, int b) {
    return a + b;
}

std::string hello_to_me(const std::string& name) {
    return std::string{"hello "} + name;
}
